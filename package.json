{
  "name": "encounterdemo",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "lite-server",
    "sass": "sass --watch quizStyle.sass:quizStyle.css",
    "minify": "uglifyjs -c -m --keep-fnames -o quiz.min.js -- quiz.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "lite-server": "^2.6.1"
  }
}
